GDX-USD:
  discrepancy: 1.0
  precision: 6
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ linea ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: gdx-token
          currency: USD

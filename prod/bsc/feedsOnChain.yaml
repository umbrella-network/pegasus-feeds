# Crypto

UMB-USD:
  discrepancy: 1.0
  precision: 8
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: umbrella-network
          currency: usd
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: UMB
          tsyms: USD

AVAX-USD:
  discrepancy: 1.0
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: AVAX
          tsym: USD
    - fetcher:
        name: CoingeckoPrice
        params:
          id: avalanche-2
          currency: USD

BNB-USD:
  discrepancy: 1.0
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: BNB
          tsym: USD
    - fetcher:
        name: CoingeckoPrice
        params:
          id: binancecoin
          currency: USD

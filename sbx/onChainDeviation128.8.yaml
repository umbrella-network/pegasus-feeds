# Prices that are 128bits with 8 decimals

UMB-USD:
  discrepancy: 1.0 # allowed discrepancy level for price signing [%]
  precision: 8 # we can choose any precision for value, but it will be stored with 8 decimals always
  heartbeat: 43200 # [s]
  trigger: 1 # [%]
  interval: 60 # [s] if not provided, it will be checked every time
  chains: [ multiversx ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: umbrella-network
          currency: USD

VC-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ polygon ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: velocore
          currency: USD

WEFI-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 1800
  trigger: 0.5
  interval: 60
  chains: [ polygon, arbitrum ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: wefi-finance
          currency: USD

ArbitrumGas-TWAP10-wei:
  discrepancy: 1.0
  precision: 9
  heartbeat: 86400
  trigger: 25.0
  interval: 60
  chains: [ arbitrum ]
  inputs:
    - fetcher:
        name: TWAPGasPrice
        params:
          twap: 10
          chainId: arbitrum

USDC-USD:
  discrepancy: 0.5
  precision: 8
  heartbeat: 21600
  trigger: 0.1
  interval: 60
  chains: [ multiversx, xdc, avax_meld, concordium, okx ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: usd-coin
          currency: USD
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: USDC
          tsyms: USD

USDT-USD:
  discrepancy: 0.5
  precision: 8
  heartbeat: 21600
  trigger: 0.1
  interval: 60
  chains: [ multiversx, avax_meld, xdc, concordium, okx ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: tether
          currency: USD
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: USDT
          tsyms: USD

WBTC-USD:
  discrepancy: 0.5
  precision: 8
  heartbeat: 86400
  trigger: 1.0
  interval: 60
  chains: [ multiversx, avax_meld, xdc, concordium, okx, rootstock ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: wrapped-bitcoin
          currency: USD
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: WBTC
          tsyms: USD

BUSD-USD:
  discrepancy: 0.5
  precision: 8
  heartbeat: 3600
  trigger: 0.5
  interval: 60
  chains: [ multiversx ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: binance-usd
          currency: USD
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: BUSD
          tsyms: USD

WETH-USD:
  discrepancy: 0.5
  precision: 8
  heartbeat: 3600
  trigger: 0.5
  interval: 60
  chains: [ multiversx, avax_meld, xdc, concordium, okx, arthera, astar ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: weth
          currency: USD

UTX-USD:
  discrepancy: 0.5
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ multiversx ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: utrust
          currency: USD

EGLD-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ multiversx ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: elrond-erd-2
          currency: USD
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: EGLD
          tsyms: USD

XRP-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ xdc ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: ripple
          currency: USD
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: XRP
          tsyms: USD

PRNT-USDT:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ xdc ]
  inputs:
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: PRNT
          tsyms: USDT

CCD-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ concordium ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: concordium
          currency: USD
    - fetcher:
        name: CryptoComparePrice
        params:
          fsym: CCD
          tsyms: USD

OKB-USD:
  discrepancy: 0.5
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ okx ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: okb
          currency: USD

RIF-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ rootstock ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: rif-token
          currency: USD

RBTC-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ rootstock ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: rootstock
          currency: USD

MOC-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ rootstock ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: mossland
          currency: USD
          
SOV-USD:
  discrepancy: 1.0
  precision: 8
  heartbeat: 86400
  trigger: 0.5
  interval: 60
  chains: [ rootstock ]
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: sovryn
          currency: USD
